name: pluto_grid
description: PlutoGrid is data grid for flutter.
version: 0.1.0
homepage: https://bosskmk.github.io

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0 <2.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.24.1
  linked_scroll_controller: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^4.1.1
